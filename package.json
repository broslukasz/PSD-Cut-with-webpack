{
  "name": "starter-js",
  "version": "1.0.0",
  "description": "",
  "main": "bundle.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:home": "cross-env PAGES=home webpack-dev-server --port 7000 --progress --colors --watch --inline",
    "start:ilustrations": "cross-env PAGES=ilustrations webpack-dev-server --port 7001 --progress --colors --watch --inline",
    "start:login": "cross-env PAGES=login webpack-dev-server --port 7002 --progress --colors --watch --inline",
    "start:error": "cross-env PAGES=error webpack-dev-server --port 7003 --progress --colors --watch --inline",
    "start:add-info": "cross-env PAGES=additional-info webpack-dev-server --port 7004 --progress --colors --watch --inline",
    "start:consultant": "cross-env PAGES=consultant webpack-dev-server --port 7005 --progress --colors --watch --inline",
    "start:needs": "cross-env PAGES=needs webpack-dev-server --port 7006 --progress --colors --watch --inline",

    "build:all": "cross-env BUILD=true PAGES=home,ilustrations,login,error,additional-info,consultant,needs webpack --config",
    "build:home": "cross-env BUILD=true PAGES=home webpack --config",
    "build:ilustrations": "cross-env BUILD=true PAGES=ilustrations webpack --config",
    "build:login": "cross-env BUILD=true PAGES=login webpack --config",
    "build:error": "cross-env BUILD=true PAGES=error webpack --config",
    "build:add-info": "cross-env BUILD=true PAGES=additional-info webpack --config",
    "build:consultant": "cross-env BUILD=true PAGES=consultant webpack --config",
    "build:needs": "cross-env BUILD=true PAGES=needs webpack --config"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "vanilla-modal": "^1.3.0"
  },
  "devDependencies": {
    "cross-env": "^3.1.3",
    "css-loader": "^0.25.0",
    "extract-text-webpack-plugin": "^1.0.1",
    "html-webpack-plugin": "^2.24.0",
    "jade": "^1.11.0",
    "jade-loader": "^0.8.0",
    "jquery": "^3.1.1",
    "node-sass": "^3.10.1",
    "raw-loader": "^0.5.1",
    "sass-loader": "^4.0.2",
    "style-loader": "^0.13.1",
    "webpack": "^1.13.3",
    "webpack-dev-server": "^1.16.2",
    "webpack-merge": "^0.15.0"
  }
}
